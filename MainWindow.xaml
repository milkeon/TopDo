<Window x:Class="TopDo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="Top Do"
        Width="520"
        Height="720"
        MinWidth="460"
        MinHeight="620"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent"
        Closing="Window_Closing"
        Loaded="Window_Loaded"
        LocationChanged="Window_StateChanged"
        SizeChanged="Window_StateChanged"
        StateChanged="Window_StateChanged">
    <Window.Resources>
        <SolidColorBrush x:Key="TextBrush" Color="#F8FAFC" />
        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="#52FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#16FFFFFF" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Height" Value="40" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#14FFFFFF" />
            <Setter Property="BorderBrush" Value="#38FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Height" Value="40" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MultilineFieldTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource FieldTextBoxStyle}">
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Height" Value="96" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
            <Setter Property="Padding" Value="12,10" />
        </Style>
        <Style x:Key="GlassActionButtonStyle" TargetType="Button" BasedOn="{StaticResource GhostButtonStyle}">
            <Setter Property="Height" Value="42" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#B57C3AED" Offset="0" />
                        <GradientStop Color="#B522D3EE" Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#7AFFFFFF" />
        </Style>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="28"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="8"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Grid Margin="10" ClipToBounds="True">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#07111F" Offset="0" />
                <GradientStop Color="#0D1535" Offset="0.45" />
                <GradientStop Color="#140E25" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Canvas IsHitTestVisible="False">
            <Ellipse Width="250" Height="250" Canvas.Left="-40" Canvas.Top="-30" Opacity="0.78">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#FFF97316" Offset="0" />
                        <GradientStop Color="#AAFB7185" Offset="0.5" />
                        <GradientStop Color="#00FB7185" Offset="1" />
                    </RadialGradientBrush>
                </Ellipse.Fill>
                <Ellipse.Effect>
                    <BlurEffect Radius="40" />
                </Ellipse.Effect>
            </Ellipse>
            <Ellipse Width="310" Height="310" Canvas.Left="250" Canvas.Top="-40" Opacity="0.84">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#FF34D399" Offset="0" />
                        <GradientStop Color="#AA22D3EE" Offset="0.58" />
                        <GradientStop Color="#0022D3EE" Offset="1" />
                    </RadialGradientBrush>
                </Ellipse.Fill>
                <Ellipse.Effect>
                    <BlurEffect Radius="55" />
                </Ellipse.Effect>
            </Ellipse>
            <Ellipse Width="220" Height="220" Canvas.Left="360" Canvas.Top="400" Opacity="0.8">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#FF60A5FA" Offset="0" />
                        <GradientStop Color="#AAA855F7" Offset="0.55" />
                        <GradientStop Color="#00A855F7" Offset="1" />
                    </RadialGradientBrush>
                </Ellipse.Fill>
                <Ellipse.Effect>
                    <BlurEffect Radius="48" />
                </Ellipse.Effect>
            </Ellipse>
            <Ellipse Width="170" Height="170" Canvas.Left="40" Canvas.Top="430" Opacity="0.74">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#FF22D3EE" Offset="0" />
                        <GradientStop Color="#AA6366F1" Offset="0.58" />
                        <GradientStop Color="#006366F1" Offset="1" />
                    </RadialGradientBrush>
                </Ellipse.Fill>
                <Ellipse.Effect>
                    <BlurEffect Radius="36" />
                </Ellipse.Effect>
            </Ellipse>
        </Canvas>

        <Border CornerRadius="28"
                Background="#1AFFFFFF"
                BorderBrush="#66FFFFFF"
                BorderThickness="1.2"
                Padding="20"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="14" Opacity="0.28" Color="#020617" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Height="34" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <TextBlock Text="Top Do"
                               VerticalAlignment="Center"
                               FontSize="28"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}" />
                </Grid>

                <Border Grid.Row="1"
                        Margin="0,18,0,16"
                        Padding="16"
                        CornerRadius="22"
                        Background="#14FFFFFF"
                        BorderBrush="#48FFFFFF"
                        BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.12" Color="#67E8F9" />
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="할 일" Foreground="#E2E8F0" Margin="0,0,0,6" />
                        <TextBox x:Name="TitleBox"
                                 Style="{StaticResource FieldTextBoxStyle}"
                                 KeyDown="TitleBox_KeyDown"
                                 ToolTip="할 일(타이틀)" />

                        <StackPanel x:Name="ComposerDetailsPanel" Visibility="Collapsed" Margin="0,12,0,0">
                            <TextBlock Text="내용" Foreground="#E2E8F0" Margin="0,0,0,6" />
                            <ItemsControl x:Name="ChecklistItemsControl"
                                          ItemsSource="{Binding ComposerChecklistItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,10,0" />

                                            <TextBox Grid.Column="1"
                                                     Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource FieldTextBoxStyle}"
                                                     Height="38"
                                                     PreviewKeyDown="ChecklistTextBox_PreviewKeyDown"
                                                     ToolTip="체크리스트 항목" />

                                            <Button Grid.Column="2"
                                                    Content="×"
                                                    Width="30"
                                                    Height="32"
                                                    Padding="0"
                                                    Margin="8,0,0,0"
                                                    Background="#14FFFFFF"
                                                    BorderBrush="#52FFFFFF"
                                                    BorderThickness="1"
                                                    Foreground="White"
                                                    Click="RemoveChecklistItem_Click"
                                                    Tag="{Binding}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Text="Enter로 항목 추가 · Shift+Enter로 완료"
                                       Foreground="#94A3B8"
                                       FontSize="11"
                                       Margin="0,6,0,0" />

                            <TextBlock Text="날짜" Foreground="#E2E8F0" Margin="0,12,0,6" />
                            <DatePicker x:Name="DueDatePicker"
                                        Height="40"
                                        Background="#14FFFFFF"
                                        BorderBrush="#38FFFFFF"
                                        BorderThickness="1"
                                        Foreground="White" />

                            <TextBlock Text="시간" Foreground="#E2E8F0" Margin="0,12,0,6" />
                            <TextBox x:Name="DueTimeBox"
                                     Style="{StaticResource FieldTextBoxStyle}"
                                     Text="09:00"
                                     ToolTip="시간(HH:mm)" />
                        </StackPanel>

                        <Button x:Name="ComposerActionButton"
                                Margin="0,14,0,0"
                                Content="추가"
                                Style="{StaticResource GlassActionButtonStyle}"
                                Click="ComposerActionButton_Click" />
                    </StackPanel>
                </Border>

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Items}"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="White">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,10"
                                    Padding="14"
                                    CornerRadius="18"
                                    Background="#12FFFFFF"
                                    BorderBrush="#44FFFFFF"
                                    BorderThickness="1">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.08" Color="#93C5FD" />
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <CheckBox IsChecked="{Binding IsDone}" VerticalAlignment="Top" Margin="0,4,0,0" />
                                    <StackPanel Grid.Column="1" Margin="12,0,12,0">
                                        <TextBlock Text="{Binding Title}"
                                                   FontSize="15"
                                                   FontWeight="SemiBold"
                                                   Foreground="White"
                                                   TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Content}"
                                                   FontSize="11"
                                                   Foreground="#E2E8F0"
                                                   TextWrapping="Wrap"
                                                   Margin="0,6,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding DueAt, StringFormat=마감: {0:yyyy-MM-dd HH:mm}}"
                                                   Foreground="#C7D2FE"
                                                   FontSize="11"
                                                   Margin="0,4,0,0" />
                                        <TextBlock Text="{Binding CreatedAt, StringFormat=생성: {0:yyyy-MM-dd HH:mm}}"
                                                   Foreground="#94A3B8"
                                                   FontSize="11"
                                                   Margin="0,2,0,0" />
                                    </StackPanel>
                                    <Button Grid.Column="2"
                                            Content="삭제"
                                            Style="{StaticResource GhostButtonStyle}"
                                            Width="60"
                                            Height="32"
                                            Padding="0"
                                            Click="DeleteButton_Click"
                                            Tag="{Binding}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>
